{
  "version": 2,
  "builds": [
    {
      "src": "webhook.py",
      "use": "@vercel/python",
      "config": { "maxLambdaSize": "50mb" }
    }
  ],
  "routes": [
    { "src": "webhook", "dest": "webhook.py" },
    { "src": "/api/setup", "dest": "webhook.py" }
  ]
}